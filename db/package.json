{
  "name": "migration",
  "type": "module",
  "scripts": {
    "migrate": "bunx --bun node-pg-migrate --env-file='./.env'  --migration-file-language ts --config-file migrate.config.ts"
  },
  "devDependencies": {
    "@types/node-pg-migrate": "^2.3.1",
    "node-pg-migrate": "^7.7.1",
    "@types/pg": "^8.20.0"
  }
}
